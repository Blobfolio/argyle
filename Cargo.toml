[package]
name = "argyle"
version = "0.9.0"
authors = ["Blobfolio, LLC. <hello@blobfolio.com>"]
edition = "2021"
rust-version = "1.81"
description = "A lightweight, agnostic CLI argument parser."
license = "WTFPL"
repository = "https://github.com/Blobfolio/argyle"
readme = "README.md"
exclude = [
	".github",
	".gitignore",
	".righteous-sandbox.json",
	"doc",
	"examples",
	"justfile",
	"release",
]

[package.metadata.docs.rs]
rustc-args = ["--cfg", "docsrs"]
rustdoc-args = ["--cfg", "docsrs"]
features = [ "commands" ]
default-target = "x86_64-unknown-linux-gnu"

[package.metadata.bashman]
name = "Argyle"
bash-dir = "./"
man-dir = "./"
credits-dir = "./"

[features]
default = []

# Enables Argument::Command variant and related methods. Use this if your app
# accepts commands in addition to -k/--key stuff.
commands = []
